# 个人博客系统

一个基于 Vue 3 + Supabase 构建的现代化个人博客系统。

## 项目特性

- 🎯 **现代技术栈**: Vue 3 + Vite + Supabase
- 📱 **响应式设计**: 完美适配桌面端和移动端
- 🔐 **安全认证**: 基于 Supabase 的安全数据存储
- 📝 **文章管理**: 完整的文章发布、编辑、展示功能
- 💬 **评论系统**: 支持用户评论和互动
- 📊 **分类管理**: 文章分类和标签管理
- 🚀 **性能优化**: 快速加载和优秀的用户体验

## 技术栈

- **前端框架**: Vue 3
- **构建工具**: Vite
- **数据库**: Supabase (PostgreSQL)
- **UI 组件**: 原生 CSS + 响应式设计
- **路由管理**: Vue Router

## 项目结构

```
src/
├── views/           # 页面组件
│   ├── Home.vue     # 首页
│   ├── Articles.vue # 文章列表页
│   └── ArticleDetail.vue # 文章详情页
├── router/          # 路由配置
│   └── index.js
├── services/        # API 服务
│   └── supabase.js
└── App.vue          # 根组件
```

## 数据库结构

### 数据表

1. **categories** - 文章分类表
   - id (主键)
   - name (分类名称)
   - description (分类描述)
   - created_at (创建时间)

2. **articles** - 文章表
   - id (主键)
   - title (文章标题)
   - content (文章内容)
   - summary (文章摘要)
   - category_id (分类ID)
   - cover_image (封面图片)
   - view_count (浏览量)
   - created_at (创建时间)
   - updated_at (更新时间)

3. **comments** - 评论表
   - id (主键)
   - article_id (文章ID)
   - author_name (作者姓名)
   - author_email (作者邮箱)
   - content (评论内容)
   - created_at (创建时间)

## 安装和运行

### 环境要求

- Node.js (版本 14 或更高)
- npm 或 yarn

### 安装依赖

```bash
npm install
```

### 环境配置

1. 复制 `.env.example` 为 `.env`
2. 配置 Supabase 连接信息：

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### 运行开发服务器

```bash
npm run dev
```

访问 http://localhost:3000 查看应用

### 构建生产版本

```bash
npm run build
```

## 功能说明

### 首页 (/)
- 显示博客简介和最新文章
- 导航到文章列表页

### 文章列表页 (/articles)
- 分页显示所有文章
- 支持按分类筛选
- 搜索功能

### 文章详情页 (/article/:id)
- 完整文章内容展示
- 阅读量统计
- 评论功能
- 相关文章推荐

## API 接口

项目使用 RESTful API 设计，所有接口返回统一格式：

```json
{
  "success": true,
  "code": 200,
  "message": "操作成功",
  "data": { ... },
  "timestamp": 1672531200000
}
```

### 主要接口

- `GET /api/v1/articles` - 获取文章列表
- `GET /api/v1/articles/:id` - 获取文章详情
- `POST /api/v1/comments` - 发表评论
- `GET /api/v1/categories` - 获取分类列表

## 部署说明

### 构建部署

1. 运行 `npm run build` 生成 dist 目录
2. 将 dist 目录部署到 Web 服务器
3. 配置服务器支持 SPA 路由

### Supabase 配置

1. 在 Supabase 控制台创建项目
2. 执行 `supabase/schema.sql` 创建数据库表
3. 配置 Row Level Security (RLS) 策略

## 开发指南

### 添加新功能

1. 在 `src/views/` 目录创建新的页面组件
2. 在 `src/router/index.js` 中添加路由配置
3. 在 `src/services/supabase.js` 中添加对应的 API 方法

### 样式规范

- 使用 CSS 变量定义主题色
- 采用移动优先的响应式设计
- 遵循 BEM 命名规范

## 贡献指南

欢迎提交 Issue 和 Pull Request 来改进项目。

## 许可证

MIT License